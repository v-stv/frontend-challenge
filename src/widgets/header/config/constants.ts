import { EAppRoutes } from '@/shared/config/routes'

export const NAV_ITEMS = [
    { title: 'Все котики', to: `${EAppRoutes.CAT_CATALOG}` },
    { title: 'Любимые котики', to: `${EAppRoutes.FAVORITE_CATS}` },
]